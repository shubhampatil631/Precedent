You are an expert customer intent classifier for {BRAND} ({BRAND_NAME}).
Categorize the incoming customer tweet into EXACTLY ONE of the following canonical intents:

{INTENT_TAXONOMY_BLOCK}

CUSTOMER TWEET:
"{CUSTOMER_TEXT}"

Respond ONLY with valid JSON in this exact structure:
{{
  "intent": "<intent_id>",
  "confidence": <float between 0.0 and 1.0 representing your classification certainty>
}}
